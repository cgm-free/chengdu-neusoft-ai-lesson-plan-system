server:
  port: 8080

spring:
  application:
    name: nsu-edu-maic-backend
  profiles:
    active: local
  servlet:
    multipart:
      location: E:/nsu-edu-maic/tmp/uploads
      max-file-size: 50MB
      max-request-size: 100MB
  datasource:
    url: jdbc:mysql://localhost:3306/nsu_edu_maic?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: ${DB_PASSWORD:}
    driver-class-name: com.mysql.cj.jdbc.Driver

maic:
  registration:
    invitation-code: ${REGISTRATION_INVITATION_CODE:}
  rate-limit:
    login:
      max-requests: ${LOGIN_RATE_LIMIT_MAX:10}
      window-minutes: ${LOGIN_RATE_LIMIT_WINDOW_MINUTES:10}
    registration:
      max-requests: ${REGISTRATION_RATE_LIMIT_MAX:5}
      window-minutes: ${REGISTRATION_RATE_LIMIT_WINDOW_MINUTES:60}
  cors:
    allowed-origin-patterns: http://localhost:*,http://127.0.0.1:*
  ai:
    provider: deepseek
    model-name: deepseek-v4-flash
    api-key: ${AI_API_KEY:}
    base-url: ${AI_BASE_URL:https://api.deepseek.com}
  ocr:
    enabled: true
    python-command: ${OCR_PYTHON:python}
    script-path: ${OCR_SCRIPT_PATH:scripts/paddle_ocr.py}
  course-plan:
    default-template-path: ${COURSE_PLAN_DEFAULT_TEMPLATE_PATH:E:/nsu-edu-maic/20XX-20XX学年第X学期《课程名称》-课程教案（模版).docx}
